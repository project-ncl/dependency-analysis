name: Build snapshot version and upload to Maven Central

permissions: {}

on:
  push:
    branches: [ "ncl-9782" ]

jobs:
    call-snapshot-job:
      uses: project-ncl/shared-github-actions/.github/workflows/maven-snapshot.yml@0fd979fcdd90cf1b62e6410221d64efd6b470865 # v0.0.21
      with:
        project_name: 'project-ncl/dependency-analysis'
      secrets:
        SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
        SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
        QUAY_DEVEL_USERNAME: ${{ secrets.QUAY_DEVEL_USERNAME }}
        QUAY_DEVEL_PASSWORD: ${{ secrets.QUAY_DEVEL_PASSWORD }}
